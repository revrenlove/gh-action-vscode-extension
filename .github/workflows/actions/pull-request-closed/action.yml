name: "On PR Closed"
description: "Prints labels for PR when the PR was merged."
# inputs:
#     who-to-greet: # id of input
#         description: "Who to greet"
#         required: true
#         default: "World"
# outputs:
#     time: # id of output
#         description: "The time we greeted you"
runs:
    using: "node20"
    main: "index.mjs"
